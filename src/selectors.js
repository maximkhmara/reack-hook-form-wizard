export const selectSize = (state) => state.size;
export const selectSause = (state) => state.sause;
export const selectCheese = (state) => state.cheese;
export const selectCategory = (state) => state.category;
export const selectImage = (state) => state.image;
